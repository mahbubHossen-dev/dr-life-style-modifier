import Login from '@/components/loginComponents/Login';
import React from 'react';

const page = () => {
    return (
        <div>
            <Login />
        </div>
    );
};

export default page;